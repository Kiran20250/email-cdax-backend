# ===============================
# ? Application Configuration
# ===============================
spring.application.name=cdaxVideo

# ===============================
# ? Database Configuration (PostgreSQL on Render)
# Render automatically provides environment variables:
# DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
# ===============================
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jackson.serialization.fail-on-empty-beans=false

# ===============================
# ? Server Configuration
# Render dynamically assigns port ? must use ${PORT}
# ===============================
server.port=${PORT:8080}

# ===============================
# ? Email SMTP Configuration (Gmail)
# Use App Password, not your real Gmail password
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kirandhumele@gmail.com
spring.mail.password=aifmxhlqfogoznai
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# ? Frontend Confirmation Link (Render Public URL)
# ?? After deploying, update this value with your Render URL
# e.g. https://cdaxvideo.onrender.com
# ===============================
app.frontend.confirmationBaseUrl=https://cdaxvideo.onrender.com

# ===============================
# ? JWT Configuration
# ===============================
jwt.secret=SgkA91mP0xQwZr7LpUuVnJtYxHc2oDfE
jwt.expirationMs=3600000

# ===============================
# ? Logging
# ===============================
logging.level.org.springframework.mail=INFO
logging.level.org.hibernate.SQL=DEBUG
